import { HoverGlow } from './hover-glow';

describe('HoverGlow', () => {
  it('should create an instance', () => {
    const directive = new HoverGlow();
    expect(directive).toBeTruthy();
  });
});
